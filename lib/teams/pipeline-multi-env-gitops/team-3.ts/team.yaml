import { ApplicationTeam } from "@aws-quickstart/eks-blueprints";
import { ArnPrincipal } from "aws-cdk-lib/aws-iam";

export class  "team-3"Team extends ApplicationTeam {
  constructor(accountID: string, environment: string) {
    super({
      name: `"team-3"-${environment}`,
      userRoleArn: `arn:aws:iam::${accountID}:role/ "team-3"`,
      namespace: ""team-3"",
    });
  }
}
